[project]
name = "vcjoinlogbot"
version = "0.1.0"
description = "A Discord bot that logs voice channel join and leave events to discord channels."
requires-python = ">=3.12"
dependencies = [
    "asyncpg>=0.31.0",
    "discord-py>=2.7.1",
    "jishaku>=2.7.5",
    "python-dotenv>=1.2.2",
]
